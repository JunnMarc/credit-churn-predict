# Credit Churn Predictor

1. Install dependencies with `pip install -r requirements.txt`, 2. train artifacts with `python train_model.py --csv data\Churn_Modelling.csv`, 3. run the app with `python -m streamlit run streamlit_app.py`.
